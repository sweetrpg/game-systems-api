apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sweetrpg-game-systems

resources:
    - ../../base
    - configmaps.yaml
    - secrets.yaml
    - ingress.yaml
    - middlewares.yaml

labels:
    - pairs:
          app: api
          version: v1
          platform: sweetrpg
          package: game-systems
          component: api

generatorOptions:
    disableNameSuffixHash: true

images:
    - name: game-systems-api
      newName: ghcr.io/sweetrpg/game-systems-api
      newTag: latest
