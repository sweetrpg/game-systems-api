---
# Plaintext placeholder for local dev only - dev/prod use Akeyless-backed ExternalSecrets
# instead (see ../dev/secrets/db.yaml).
apiVersion: v1
kind: Secret

metadata:
  name: api-db

stringData:
  DB_PW: local-dev-password
